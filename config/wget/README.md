need some symlinks (adapt paths if needed):
```
mkdir ~/.config/wget
ln -s ~/utils/config/wget/wgetrc ~/.config/wget/wgetrc # bind wget conf file from this git repo to the "expected location" (set through the WGETRC global variable)
```
